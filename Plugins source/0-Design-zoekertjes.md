# Zoekertjes weergeven voor designers

## Data van een zoekertje

Zoekertjes hebben:
* Title (default WordPress)
* _adName (via get_post_meta())
* _adLocation (via get_post_meta())
* _adDescription (via get_post_meta())
